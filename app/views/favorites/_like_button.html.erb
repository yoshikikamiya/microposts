<% unless current_user ==micropost %>
 <% if current_user.like_microposts?(micropost) %>
  <%= form_with(model: current_user.favorites.find_by(like_id: micropost.id), local: true,method: :delete) do |f| %>
   <%= hidden_field_tag :like_id, micropost.id %>
  <%= sumbit 'Unlike' , class: 'btn btn-danger btn-sm' %>
   <% end %>
  <% else %>
   <%= form_with(model: current_user.ravorites.build, local: true) do |f| %>
    <%= hidden_field_tag :like_id, micropost,id %>
    <%= f.submit 'Like',class: 'btn btn-primary btn-sm' %>
    <% end %>
   <% end %>
   <% end %>